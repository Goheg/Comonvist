<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Comonvist - Welcome</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
        }
        .navbar-brand {
            font-size: 1.5rem;
            font-weight: bold;
        }
        .user-visit {
            background-color: #fff;
            border-radius: 8px;
            padding: 20px;
            margin-bottom: 20px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .user-visit h3 {
            color: #007BFF;
            margin-bottom: 10px;
        }
        .comment {
            font-style: italic;
            color: #6c757d;
        }
        .user-post {
            background: #fff;
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 20px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            position: relative;
            min-height: 150px; /* Ensure cards have a minimum height */
        }
        .user-post p {
            color: #555;
            margin: 0;
            padding-top: 40px; /* Add top padding to center the comment vertically */
        }
        .place-tag {
            position: absolute;
            top: 10px;
            left: 10px;
            background: #28a745;
            color: #fff;
            padding: 5px 10px;
            border-radius: 5px;
            font-size: 0.9rem;
            font-weight: bold;
        }
        .post-footer {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-top: 15px;
            font-size: 0.9rem;
            color: #777;
            position: absolute;
            bottom: 10px;
            left: 20px;
            right: 20px;
        }
        .post-date {
            text-align: left;
        }
        .post-author {
            text-align: right;
            font-weight: bold;
            color: #007BFF;
        }
    </style>
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">Comonvist</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/login">Login</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/register">Register</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <div class="container my-5">
        <h1 class="text-center mb-4">Welcome to Comonvist</h1>
        <div class="row">
            <div class="col-md-8 mx-auto">
                <!-- User Visits Section -->
                <!-- Post 1 -->
                <% if (locals.user) { %>
                    <% for( let i = 0; i < user.length; i++ ) { %>
                  <div class="user-post">
                  <div class="place-tag"><%= user[i].places %></div>
                  <p><%= user[i].comments %></p>
                  <div class="post-footer">
                      <div class="post-date"><%= user[i].created_at %></div>
                      <div class="post-author"><%= user[i].username %></div>
                  </div>
              </div>
               <% } %>  
               <% } %>
                <!-- <div class="user-visit">
                    <h3>Jane Smith visited Tokyo</h3>
                    <p><strong>Date:</strong> 2023-09-25</p>
                    <p class="comment">"Tokyo is vibrant and full of life. Loved the food!"</p>
                </div>
                <div class="user-visit">
                    <h3>Alex Johnson visited New York</h3>
                    <p><strong>Date:</strong> 2023-09-15</p>
                    <p class="comment">"New York never sleeps. Central Park was so peaceful."</p>
                </div> -->
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>